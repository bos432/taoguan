# 订单状态转换事实基线

更新日期：2026-08-25

| 业务事件 | 前置状态 | 当前结果 | 当前入口 |
|---|---|---|---|
| 创建订单 | 无 | 待付款、未支付 | `confirmOrder` |
| 微信支付成功 | 待付款、未支付 | 待发货、已支付 | 微信回调 `orderNotify` |
| 凭证支付通过 | 待付款、未支付 | 完成、已支付 | `orderPayAuth` |
| 凭证支付驳回 | 待付款、未支付 | 取消、支付拒绝 | `orderPayAuth` |
| 发货 | 待发货 | 待收货 | `delivery` |
| 自提核销 | 待发货 | 待评价 | `takeDelivery` |
| 确认收货 | 待收货 | 待评价 | `confirmReceipt` |
| 评价 | 待评价 | 完成 | `submitEvaluation` |
| 申请售后 | 完成 | 售后、申请中 | `submitService` |
| 同意/拒绝售后 | 售后、申请中 | 售后状态 2/3 | `serviceOrder` |
| 完成退款 | 售后 | 已退款 | `serviceOrder` |
| 买家取消 | 待付款、未支付 | 软删除，状态仍为待付款 | `cancelOrder` |

当前异常语义必须通过兼容层保留：凭证支付通过直接完成；买家取消与凭证驳回使用不同的数据表示。阶段二事件流水统一记录 `order.canceled`，但不得在迁移期批量改写历史状态。

所有后续状态写入必须先通过 `OrderStateTransitionPolicy`，并与业务事件在同一事务内提交。微信回调、支付审核、核销和退款是首批接入入口。
